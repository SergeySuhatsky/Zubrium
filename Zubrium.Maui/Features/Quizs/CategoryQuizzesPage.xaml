<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Zubrium.Maui.Views.Controls"
             xmlns:domain="clr-namespace:Zubrium.Domain"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             xmlns:local="clr-namespace:Zubrium.Maui.Features.Quizs"
             x:Class="Zubrium.Maui.Features.Quizs.CategoryQuizzesPage"
             x:DataType="local:CategoryQuizzesViewModel"
             x:Name="CategoryQuizzesPageRoot"
             BackgroundColor="#F4F5F8">

    <ScrollView>
        <VerticalStackLayout Padding="20,20,20,40" Spacing="20">
            <controls:DesktopBackButton />

            <Label Text="{Binding CategoryName, StringFormat='Квизы: {0}'}" FontSize="24" FontAttributes="Bold" TextColor="#1C1C1E" />

            <CollectionView ItemsSource="{Binding Quizzes}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="domain:QuizBlock">
                        <Border BackgroundColor="White" Stroke="#EAECEF" StrokeThickness="1" Padding="16" Margin="0,0,0,12">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="16" />
                            </Border.StrokeShape>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding BindingContext.StartQuizCommand, Source={x:Reference CategoryQuizzesPageRoot}}" CommandParameter="{Binding Id}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto, *">
                                <Image Grid.Column="0" Source="{mi:Material Icon=Assignment, IconColor=#6278E6}" WidthRequest="24" HeightRequest="24" Margin="0,0,12,0" VerticalOptions="Center"/>
                                <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                    <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" TextColor="#1C1C1E" />
                                    <Label Text="{Binding Questions.Count, StringFormat='Вопросов: {0}'}" FontSize="13" TextColor="#8E8E93" />
                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
